/*
// $data server
get$router('database').all('/*', (req, res, next) => {
	dir(decodeURIComponent(req.url));
	next();
});
*/
/*
// and $code server
get$router('codebase').all('/*', (req, res, next) => {
	dir(decodeURIComponent(req.url));
	next();
});
*/
