/*
// $data server
$data.router.all('/*', (req, res, next) => {
	dir(decodeURIComponent(req.url));
	next();
});
*/
/*
// and $code server
$code.router.all('/*', (req, res, next) => {
	dir(decodeURIComponent(req.url));
	next();
});
*/
